export const environment = {
    production: true, //Esta en produccion (esta subido en nuestro repositorio remoto
    apiUrl: ''        //(BACKEND)Url que me lo otorga el servidor remoto (AWS, Google cloud, Azure, Digital Ocean, etc.)
};
